<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- docs/06_MOBILE_SPEC.md §Android config -->

    <!-- Talk to the Mausam backend (REST + WebSocket). -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- "Use my location" on the onboarding location page and the home refresh. -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>

    <!-- android:usesCleartextTraffic is demo/prototype only: the backend is reached over
         plain http on a LAN IP (e.g. http://192.168.1.7:8000) or via `adb reverse`.
         A production build would serve the API over https and drop the attribute. -->
    <application
        android:label="Mausam Personalized"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Home-screen widget (docs/06_MOBILE_SPEC.md §Home-screen widget). Renders the last
             /home snapshot with RemoteViews and needs no permission of its own — the hourly
             refresh reuses INTERNET, declared above.

             exported="true" is the convention for an AppWidgetProvider: the only thing that
             broadcasts APPWIDGET_UPDATE to it is the launcher/AppWidget framework, and the
             receiver does nothing but redraw from app-private storage. -->
        <receiver
            android:name=".MausamWidgetProvider"
            android:label="@string/widget_label"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/mausam_widget_info" />
        </receiver>

        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
